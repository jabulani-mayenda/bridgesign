services:
  - type: web
    name: bridgesign
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: BRIDGESIGN_HOST
        value: 0.0.0.0
